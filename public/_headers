# Đã bỏ COEP/COOP vì khiến browser chặn _next/static/chunks/*.js (blocked:COEP-frame)
# và model không tải được. ONNX Runtime WASM chạy được không cần COEP.
# Nếu sau này cần SharedArrayBuffer thì dùng credentialless hoặc serve COEP cho mọi response.
